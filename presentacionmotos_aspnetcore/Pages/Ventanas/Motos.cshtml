@page
@model MotosModel
@{
    ViewData["Title"] = "Motos";
}

<center>
    <div>
        <form method="POST"
              enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div align="left">
                <h3>Motos</h3>
            </div>

            <div>
                <input type="submit"
                       value="Refrescar"
                       class="btn btn-secondary"
                       asp-page-handler="BtRefrescar" />
                <input type="submit"
                       value="Nuevo"
                       class="btn btn-secondary"
                       asp-page-handler="BtNuevo"
                       disabled="@(Model.Moto != null)" />
                <input type="submit"
                       value="Guardar"
                       class="btn btn-secondary"
                       asp-page-handler="BtGuardar"
                       disabled="@(Model.Moto == null)" />
            </div>

            @if (Model.Moto != null)
            {
                <input type="hidden"
                       asp-for="@Model.Moto!.Id" />

                <div>
                    
                    <div class="form-group">
                        <label>
                            Placa
                        </label>
                        <div class="col-6">
                            <input id="inPlaca"
                                   class="cts_forms form-control"
                                   asp-for="@Model.Moto!.Placa" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Modelos
                        </label>
                        <div class="col-6">
                            <select asp-for="@Model.Moto!.ModelosId">
                                @foreach (var v in Model.ListaModelos!)@*esto nos muestra el listado de Modelos en el select del formulario *@
                                {
                                    <option value="@v.Id">@v.Referencia - @v.Id</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Marcas
                        </label>
                        <div class="col-6">
                            <select asp-for="@Model.Moto!.MarcasId">
                                @foreach (var v in Model.ListaMarcas!)@*esto nos muestra el listado de Marcas en el select del formulario *@
                                {
                                    <option value="@v.Id">@v.Nombre - @v.Id</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Valor
                        </label>
                        <div class="col-6">
                            <input id="inValor"
                                   asp-for="@Model.Moto!.Valor" />
                        </div>
                    </div>
                </div>
            }
            
            @if (ViewData.ContainsKey("Mensaje"))
            {
                @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
                ;
            }

            @if (Model.Moto != null && Model.Borrando)
            {
                <div class='popup'>
                    <div class='pp_ms_inner'>
                        <div style='width:95%'>
                            <h2 align="left">
                                Mensaje
                            </h2>

                            <h6 align="left">
                                Desea borrar la Moto?
                            </h6>
                            <h6 align="left">
                                @Model.Moto!.Id
                            </h6>
                            
                            <input type="submit"
                                   value="Cerrar"
                                   asp-page-handler="BtCerrar"
                                   class="btn btn-secondary" />
                            <input type="submit"
                                   value="Borrar"
                                   asp-page-handler="BtBorrar"
                                   class="btn btn-secondary" />
                        </div>
                    </div>
                </div>
            }

            @if (Model != null &&
                 Model.Lista != null &&
                 Model.Lista.Count > 0)
            {
                <table class="tb_data table table-striped table-bordered table-hover">
                    <tr>
                        <th>Id</th>
                        <th>Placa</th>
                        <th>Modelos</th>
                        <th >Marcas</th>
                        <th > Valor</th>
                        <th>Acciones</th>
                    </tr>
                    @foreach (var elemento in Model.Lista)
                    {
                        <tr>
                            <td>
                                @elemento.Id
                            </td>
                            <td>
                                @elemento.Placa
                            </td>
                            <td>
                                @elemento._Modelos?.Referencia
                            </td>
                            <td >
                                @elemento._Marcas?.Nombre
                            </td>
                            <td>
                                @elemento.Valor
                            </td>
                            <td>
                                <input type="submit"
                                       value="Modicar"
                                       asp-page-handler="BtModificar"
                                       class="btn btn-secondary"
                                       asp-route-data="@elemento.Id" />
                                <input type="submit"
                                       value="Borrar"
                                       asp-page-handler="BtBorrarVal"
                                       class="btn btn-secondary"
                                       asp-route-data="@elemento.Id" />
                            </td>
                        </tr>
                    }
                </table>
            }
        </form>
    </div>
</center>
